# simulator

Synthetic data generator for VidyutDrishti. Produces 15-minute meter
readings and DT-level kWh-in for a configurable number of DTs, meters,
and days, with injected theft scenarios tracked as ground truth.

The CLI entrypoint (`python -m simulator.generate`) is implemented in
**Feature 02**.
